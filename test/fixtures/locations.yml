# Read about fixtures at http://ar.rubyonrails.org/classes/Fixtures.html

<% for i in 1..50 %>
  location_<%= i %>:
    id: <%= Fixtures.identify("location_#{i}".to_sym) %>
    name: Location_<%= i %>
    address_id: <%= Fixtures.identify("address_#{i}".to_sym) %>
<% end %>

